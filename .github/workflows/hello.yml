name: hello
on: push

jobs:
  build1:
    name: Build1
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: echo "Hello, World"
  build2:
    name: Build2
    needs: [build1]
    if: success() || skipped()
    steps:
      - run: echo "Started"
