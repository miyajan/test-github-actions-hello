name: hello
on: push

jobs:
  build:
    name: Greeting
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello, World"
      - uses: actions/cache@v1.1.0
        with:
          path: ~/
          key: v1-cache-${{ github.ref }}-key
          restore-keys: |
            cache-
