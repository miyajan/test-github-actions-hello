name: hello
on: push

jobs:
  build1:
    name: Build1
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: echo "Hello, World"
  build2:
    name: Build2
    runs-on: ubuntu-latest
    needs: [build1]
    if: needs.build1.result == 'success' || needs.build1.result == 'skipped'
    steps:
      - run: echo "Started"
