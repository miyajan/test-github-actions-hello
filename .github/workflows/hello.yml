name: hello
on: push

jobs:
  build:
    name: Greeting
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout
      - uses: miyajan/javascript-action-hello
      - run: echo "Hello, World"
      - run: |
          echo "hoge"
          echo "fuga"
